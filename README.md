# Tredence_casestudy
In real-world deployments, neural networks must be efficient in terms of memory and computation. A common technique to achieve this is pruning, where less important weights are removed.  This project implements a self-pruning neural network that learns to prune its own weights during training, instead of doing it after training.
